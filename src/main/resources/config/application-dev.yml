server:
  port: 8080
spring:
  #数据库连接配置
  datasource:
    url: jdbc:mysql://server.moeworkshop.com:3306/Aoxzon?useAffectedRows=TRUE&characterEncoding=utf-8&useSSL=false
    username: AoxzonDev
    password: 1qaz2wsx
    driver-class-name: com.mysql.cj.jdbc.Driver

#jpa的相关配置
  jpa:
    hibernate:
      ddl-auto: update #自动更新
    show-sql: true  #日志中显示sql语句
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect